import React from 'react';
import {useState, useEffect} from 'react';

const User = () => {
    const [user, setUsers] = useState([]);

    useEffect(()=>{}, [])
    return (
        <div>
            User
        </div>
    );
};

export {User};